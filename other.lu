let Person = {}
function Person.new(name, age) {
    return setmeta({
        name = name,
        age = age,
        greet = function (self) {
            return str("Hello! My name is ", self.name, " and I'm ", str(self.age), " years old.")
        }
    }, {
        __name = "person"
    })
}

return Person